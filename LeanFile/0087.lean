import Mathlib
/-! Question:
1. If $R_{R} M_{S}$ and ${ }_{R} N$, show that $\operatorname{Hom}_{R}(M, N)$ is a left $S$-module via $(s f)(m)=f(m s)$
-/
import Mathlib.Tactic

/- If R_RM_S and _RN, show that Hom_R(M, N) is a left S-module via (sf)(m)=f(ms) -/

-- in Mathlib, left S-module M is realized as \"Module S M\

/-! Informal proof:
Simply verify
-/

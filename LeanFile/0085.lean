import Mathlib
/-! Question:
3. If $M_{R}$ and ${ }_{S} N_{R}$, show that $\operatorname{Hom}_{R}(M, N)$ is a left $S$-module via $(s f)(m)=s f(m)$.
-/
import Mathlib.Tactic

/- If M_R and _SN_R, show that Hom_R(M, N) is a left S-module via (sf)(m)=sf(m) -/

-- in Mathlib, left S-module M is realized as \"Module S M\

/-! Informal proof:
Simply verify
-/

import Mathlib
/-! Question:
4. If $S_{S} M$ and ${ }_{S} N_{R}$, show that $\operatorname{Hom}_{S}(M, N)$ is a right $R$-module via $(f r)(m)=f(m) r$.
-/
import Mathlib.Tactic

/- If S_SM and _SN_R, show that Hom_S(M, N) is a right R-module via (fr)(m)=f(m)r -/

-- in Mathlib, left S-module M is realized as \"Module S M\

/-! Informal proof:
Simply check.
-/
